services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: organizaai
      POSTGRES_USER: organizaai
      POSTGRES_PASSWORD: organizaai
    ports:
      - "5432:5432"
    volumes:
      - organizaai-postgres-data:/var/lib/postgresql/data

volumes:
  organizaai-postgres-data:
