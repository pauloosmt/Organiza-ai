// Origem do backend no Render. Se o nome do serviço mudar (ex.: "organizaai-api"
// já estava em uso ao criar o serviço), atualize aqui e no connect-src da CSP em
// src/index.prod.html com o mesmo valor.
export const environment = {
  apiUrl: 'https://organizaai-api.onrender.com/api'
};
